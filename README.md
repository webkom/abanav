## A collection of sites for Abakus

## To start the project, run

`yarn install`

`yarn start`

## To add new sites, edit the [sites.json](src/sites.json) file with a new object containing the same fields as the existing objects.
